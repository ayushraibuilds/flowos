import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('FlowOS app starts', (WidgetTester tester) async {
    // Basic smoke test — will be expanded in later phases
    expect(1 + 1, 2);
  });
}
